# Rystra（Rust 反向代理工具）

Rystra 是一个使用 **Rust** 编写的、**自主可控、强工程化、可长期演进** 的反向代理系统。

项目在 **MVP 阶段即采用“为未来提前拆分多个 crate”** 的组织方式，通过清晰的边界划分、严格的依赖规则和插件化架构，避免在后期扩展协议、传输层、多路复用、租户隔离等能力时出现大规模重构。

当前阶段重点目标：  
👉 **TCP 反向代理 MVP 已完成**  
👉 **稳定性与运维优化已完成**  
👉 **开始扩展能力阶段（TLS 支持）**

> 当前状态：✅ Phase 1 完成 / ✅ Phase 2 完成 / 🚧 Phase 3 进行中

---

## ✨ 设计目标

- **自主可控**
  - 控制协议、状态机、插件体系完全自研
  - 不依赖重量级 RPC 框架
- **工程化优先**
  - 多 crate 拆分，职责边界清晰
  - 明确依赖方向，避免“核心腐化”
- **插件化架构**
  - Proxy / Transport / Auth / Hook /（规划）Mux 全部插件化
  - MVP 阶段仅启用内建插件
- **可演进**
  - 协议版本化
  - 能力按插件逐步叠加
- **可运维**
  - 支持运行期 reload（无需自动监听文件）
  - 连接级别可观测性

---

## 📦 Workspace 结构（为未来提前拆分）

> 即使在 MVP 阶段，也保留完整 crate 骨架  
> 未实现能力以 **占位 + 文档约定** 的形式存在

```
rystra/
├── Cargo.toml
├── crates/
│  ├── rystra-model/                # 基础数据结构、ID、错误码（最底层）
│  ├── rystra-proto/                # 控制面协议定义（版本化）
│  ├── rystra-plugin/               # 插件 trait 定义（Proxy / Transport / Auth / Hook / Mux）
│  ├── rystra-plugin-registry/      # 插件注册与装配
│  ├── rystra-core/                 # 核心状态机、ProxyManager、RuntimeController
│  ├── rystra-net/                  # 网络工具（io copy、framing、连接ID）
│  ├── rystra-runtime/              # 运行时抽象（任务管理、超时、重连）
│  ├── rystra-config/               # 配置加载、校验、snapshot 配置结构体 + 校验 + 语义化类型
│  ├── rystra-observe/              # tracing / metrics 统一封装
│  ├── rystra-server/               # Server 二进制
│  ├── rystra-client/               # Client 二进制
│  ├── rystra-cli/                  # CLI / 管理工具（可选）
│  ├── rystra-testkit/              # 集成测试工具
│  └── plugins/
│     ├── proxy/
│     │  ├── rystra-proxy-tcp/      # ✅ MVP
│     │  ├── rystra-proxy-http/     # 规划
│     │  └── rystra-proxy-udp/      # 规划
│     ├── transport/
│     │  ├── rystra-transport-tcp/  # ✅ MVP
│     │  ├── rystra-transport-tls/  # 规划
│     │  └── rystra-transport-quic/ # 规划
│     ├── auth/
│     │  ├── rystra-auth-token/     # ✅ MVP
│     │  └── rystra-auth-mtls/      # 规划
│     ├── hook/
│     │  └── rystra-hook-rate/      # 规划
│     └── mux/
│        └── rystra-mux-smux/       # 规划
```

---

## 🔒 依赖规则（必须遵守）

依赖方向（自下而上）：

1. `rystra-model`
2. `rystra-proto`
3. `rystra-plugin`
4. `plugins/*`
5. `rystra-core`
6. `rystra-server / rystra-client / rystra-cli`

**禁止行为：**

- `model` 依赖任何上层 crate
- `proto` 依赖 `core/server/client`
- 插件之间直接互相依赖

---

## 🔌 插件系统设计

### 插件类型

- **Proxy Plugin**：tcp（MVP）、udp/http（规划）
- **Transport Plugin**：tcp（MVP）、tls/quic（规划）
- **Auth Plugin**：token（MVP）、mTLS（规划）
- **Hook Plugin**：限速、审计、改写（规划）
- **Mux Plugin**：多路复用（规划）

### 插件原则

- 基于 Trait + Registry
- 编译期注册（无动态库加载）
- 插件代码不支持热替换
- 插件**配置**支持运行期 reload
- 

---

## 🗺️ Roadmap（按可交付能力）

### Phase 0：工程骨架 ✅

- [x] Workspace + crate 拆分
- [x] 依赖规则文档化
- [ ] testkit 骨架

### Phase 1：TCP 反向代理 MVP ✅

- [x] 控制面协议（Hello / Auth / Heartbeat / RegisterProxy / OpenStream / StreamReady）
- [x] TCP Transport 插件
- [x] Token Auth 插件
- [x] Server ↔ Client TCP 反向代理闭环
- [x] 单端口复用（控制连接 + 数据连接）

### Phase 2：稳定性与运维 ✅

- [x] 运行期 reload（HTTP 管理接口）
- [x] graceful shutdown（Ctrl+C 信号处理）
- [x] tracing 日志系统
- [x] 配置文件控制日志级别
- [x] 客户端自动重连（指数退避）
- [x] 心跳超时检测

### Phase 3：扩展能力 🚧

- [ ] TLS Transport（进行中）
- [ ] QUIC Transport
- [ ] 多路复用（Mux）
- [ ] UDP / HTTP Proxy
- [ ] Hook 插件链
- [ ] 租户隔离 / ACL（规划）

---

## 🛠️ 技术栈

- Rust
- tokio
- serde
- tracing
- rustls（规划）
- quinn（规划）

---

## 🚧 当前状态

- ✅ Phase 1 完成：TCP 反向代理 MVP 可用
- ✅ Phase 2 完成：稳定性与运维优化（含配置热重载）
- 🚧 Phase 3 进行中：扩展能力（TLS Transport）
- Server 和 Client 可正常运行，支持 TCP 代理转发和运行期配置重载

---

## 🚀 快速开始

### 启动 Server

```bash
cargo run -p rystra-server
```

### 启动 Client

```bash
cargo run -p rystra-client
```

### 配置文件

- Server: `crates/rystra-config/server.toml`
- Client: `crates/rystra-config/client.toml`