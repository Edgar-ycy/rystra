## 第一步：安装 cargo-zigbuild

~~~powershell
cargo install cargo-zigbuild
~~~

## 第二步：安装 zig（通过 scoop 或手动下载）

~~~powershell
winget install zig.zig
~~~

## 第三步：执行交叉编译

~~~powershell
cargo zigbuild --release --target x86_64-unknown-linux-gnu -p rystra-server -p rystra-client
~~~

